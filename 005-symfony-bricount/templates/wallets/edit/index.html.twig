{% extends '_layouts/in_app.html.twig' %}

{% block title %}Modification de {{ wallet.label }} | +Count{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/forms.css') }}">
{% endblock %}


{% block content %}
    <div class="form-page-container">
        <div class="form-card-in-app">
            <h2 class="auth-title">Modifier le Portefeuille</h2>
            <p class="auth-subtitle">Mise à jour des informations de "{{ wallet.label }}"</p>

            {{ form_start(form) }}
            <div class="input-group-custom">
                {{ form_widget(form.name, {'attr': {'class': 'form-input', 'placeholder': ' '}}) }}
                {{ form_label(form.name, null, {'label_attr': {'class': 'form-label'}}) }}
                {{ form_errors(form.name) }}
            </div>

            <button type="submit" class="btn-modern">
                Enregistrer les modifications <i class="fa-solid fa-check"></i>
            </button>

            <div style="text-align: center; margin-top: 20px;">
                <a href="{{ path('wallets_show', {uid: wallet.uid}) }}" class="btn-back">Annuler</a>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
